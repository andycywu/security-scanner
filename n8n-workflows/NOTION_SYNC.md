# 📝 Notion → Social Media 同步設定

## 🔄 流程圖

```
┌─────────────────┐
│  Schedule Trigger│ (每 1 小時檢查)
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Notion Database │ (取得新文章)
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Parse Content   │ (解析標題、摘要、標籤)
└────────┬────────┘
         │
    ┌────┼────┐
    │         │
    ▼         ▼
┌───────┐ ┌───────────────┐
│Format │ │Format FB/Threads│
└───┬───┘ └───────┬───────┘
    │             │
    ▼             │
┌───────┐         │
│Twitter│         │
└───┬───┘         │
    │             │
    └──────┬──────┘
           │
    ┌─────┴─────┐
    │           │
    ▼           ▼
┌───────┐ ┌────────┐
│Facebook│ │ Threads│
└───┬───┘ └───┬────┘
    │         │
    └────┬────┘
         │
         ▼
┌─────────────────┐
│ Update Notion   │ (標記為已發布)
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Telegram Alert  │ (通知已完成)
└─────────────────┘
```

---

## 📋 Notion Database 設定

建立一個 Database 包含以下欄位：

| 欄位名稱 | 類型 | 說明 |
|----------|------|------|
| Name | Title | 文章標題 |
| Summary | Text | 簡短摘要（用於社群媒體） |
| Tags | Multi-select | 標籤（自動轉為 #標籤） |
| Slug | Text | URL Slug |
| Status | Select | Draft / Published |
| Publish to Social | Checkbox | 勾選後自動發布 |
| Published At | Date | 發布時間 |
| Domain | Text | 你的 Blog 網域 |

---

## 🔗 申請 Notion API

1. 去 https://www.notion.so/my-integrations
2. 點 **「New integration」**
3. 填入名稱：`Security Scanner Bot`
4. 取得 **Internal Integration Token**
5. 在你的 Database 中：
   - 點右上角 **「...」** → **「Add connections」**
   - 選擇你的 Integration

---

## 🚀 n8n 設定步驟

### 1. 匯入 Workflow

1. 打開 https://n8n.andycywu.cc
2. 匯入 `notion-social-sync.json`

### 2. 設定 Credentials

#### Notion
- Credential Type: **Notion API**
- Token: [你的 Integration Token]

#### Twitter
- 同之前設定

#### Facebook
- 同之前設定

#### Threads
- 同之前設定

#### Telegram
- 同之前設定

### 3. 設定 Database ID

1. 打開你的 Notion Database
2. 複製網址中的 ID：
   ```
   https://www.notion.so/yourworkspace/12345678-1234-1234-1234-123456789012?v=...
   ```
   ID 是：`12345678-1234-1234-1234-123456789012`
3. 貼到 n8n 中的 `DATABASE_ID` 欄位

---

## 🧪 測試方式

1. 在 Notion 中建立新文章
2. 設定 Status 為 `Draft`
3. 勾選 `Publish to Social`
4. 手動執行 Workflow
5. 檢查所有社群媒體是否收到發文

---

## 💡 使用方式

1. **在 Notion 寫文章**
2. **勾選「Publish to Social」**
3. **n8n 自動發布到所有平台**
4. **Status 自動變成「Published」**

---

## ⚠️ 注意事項

- Notion API 有 rate limit（每分鐘 3 次請求）
- Twitter/Threads 每天發文次數有限制
- 建議 Schedule 設為每 1-2 小時檢查一次

---

*建立日期: 2026-02-13*
